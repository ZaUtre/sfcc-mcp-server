# SFCC Configuration
SFCC_ADMIN_CLIENT_ID=your_client_id
SFCC_ADMIN_CLIENT_SECRET=your_client_secret
SFCC_API_BASE=https://hostname.commercecloud.salesforce.com

# Server Configuration
PORT=3000
LOG_LEVEL=info
NODE_ENV=development

# MCP Server Mode (stdio or remote)
MCP_MODE=stdio
REQUEST_ID=sfcc-mcp-server

# Authentication (for remote mode)
# These would be used in a production setup with a real auth provider
AUTH_SECRET=your_auth_secret_here
ALLOWED_TOKENS=token1,token2,token3